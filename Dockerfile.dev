FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy application files
COPY . .

# Expose ports (server and vite dev)
EXPOSE 3001 5173

# Start in dev mode
CMD ["npm", "run", "dev"]
